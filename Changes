Revision history for {{$dist->name}}

{{$NEXT}}
	Read option 'profile' from Git config. 'profile' points to PerlCritic rc file. File path is (should be) relational to repository root.
	Changes to accommodate PerlCritic.
